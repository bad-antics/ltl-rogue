// ltl-rogue/main.ltl
//
// Tiny roguelike — procedurally generated dungeons, FOV, A* pathing.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-rogue v0.1.0")
    io::println("Tiny roguelike — procedurally generated dungeons, FOV, A* pathing.")
    0
}
