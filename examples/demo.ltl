// examples/demo.ltl — runnable demo for ltl_rogue

import std::io
use ltl_rogue as lib

fn main() {
    io::println("ltl_rogue demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}